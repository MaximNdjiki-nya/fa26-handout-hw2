"""HW2 Question 5

Please implement the function below to match the documentation."""

def filter_lines_containing(input_filepath: str, output_filepath: str, keyword: str) -> None:
    """
    Read the file at `input_filepath` and output only the lines that contain `keyword`
    as a substring (case-insensitive), in their original order, to a file at `output_filepath`.
    Ignore lines that do not contain the keyword. If there is already a file at that location, 
    overwrite it.

    Parameters:
        input_filepath : str
            The path to the input file to be read
        output_filepath : str
            The path to the output file to be written
        keyword : str
            The keyword to search for within the file lines
    """
    pass
