"""HW2 Question 3

Please implement the function below to match the documentation."""

from typing import Optional

def get_max_num() -> Optional[float]:
    """
    Repeatedly prompt the user to enter numbers until they enter an empty string.
    Keep track of the maximum number entered. Reject anything that isn't a valid
    number (catch the ValueError) and print a message asking again. Return the
    maximum number entered as a float, or None if no valid numbers were entered.

    Returns:
        Optional[float]: The maximum number entered by the user, or None if no 
                valid numbers were entered.
    """
    pass
